1.文件后缀的无用

liunx中后缀不强绑定打开方式，你想用什么打开就用什么打开，后缀给人看的，让你知道用什么打开。

2.程序的权限

-r    可读  （打开用任何方式）
-w    可写  （修改 用任何方式）
-x    可执行 （能否成为 命令 ）


程序命令速查

1. 创建文件

touch a.txt        # 创建空文件 a.txt
vi test.sh         # 用编辑器创建并编辑文件
echo hello > b.txt # 创建 b.txt 并写入内容

2.2. 创建文件夹
mkdir dir1         # 创建文件夹 dir1
mkdir -p a/b/c     # 递归创建多层文件夹

3. 删除文件 / 文件夹
rm a.txt           # 删除文件
rm -f b.txt        # 强制删除，不提示
rm -r dir1         # 删除文件夹及里面所有内容
rm -rf dir2        # 强制递归删除（慎用）

4. 复制文件 / 文件夹

cp a.txt c.txt              # 复制文件
cp -r dir1 dir2             # 复制整个文件夹
cp a.txt /home/             # 复制到指定目录

5. 移动 / 重命名
mv a.txt d.txt              # 重命名文件
mv dir1 /home/              # 移动文件夹


6. 修改文件权限（重点）

chmod +x test.sh            # 给文件加上 可执行权限
chmod -x test.sh            # 去掉可执行权限
chmod 755 test.sh           # 常用权限：所有者可读可写可执行，其他人可读可执行
chmod 644 a.txt             # 普通文件权限

ls -l                      # 查看文件详细信息（含权限）
